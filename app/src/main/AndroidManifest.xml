<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hoxyu.team3_weatherapp_final">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SlashCreenActivity"
            android:theme="@style/AppTheme2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Detail_Weather_Activity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Weather_FutureActivity"
            android:theme="@style/AppTheme3" />
        <activity
            android:name=".ChooseAddressActivity"
            android:theme="@style/AppTheme3" />
        <activity
            android:name=".WeatherByAddressActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Weather_Future2_Activity"
            android:theme="@style/AppTheme3" />
        <activity
            android:name=".Weather_Future_Test_Activity"
            android:theme="@style/AppTheme3" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Weather_Map_Activity"
            android:label="@string/title_activity_weather__map_"
            android:theme="@style/AppTheme2"></activity>
        <activity android:name=".BieuDoActivity"
            android:theme="@style/AppTheme3"></activity>
    </application>

</manifest>